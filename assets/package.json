{
  "private": true,
  "name": "meridian-web-sdk-assets",
  "version": "0.2.0",
  "description": "Assets for the Meridian Web SDK",
  "main": "index.js",
  "scripts": {
    "deploy": "bash bin/deploy.sh",
    "minify": "svgo --disable=removeViewBox placemarks/*.svg",
    "postversion": "npm run -s deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Hewlett Packard Enterprise Development LP",
  "license": "MIT",
  "devDependencies": {
    "svgo": "^2.8.0"
  }
}
